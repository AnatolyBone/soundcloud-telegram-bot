<div class="card card-success card-outline">
    <div class="card-header">
        <h3 class="card-title">🚀 Новая рассылка</h3>
    </div>
    <div class="card-body">
        <form method="POST" action="/broadcast" enctype="multipart/form-data" novalidate>
            <div class="mb-3">
                <label for="broadcastMessage" class="form-label">Текст сообщения</label>
                <textarea id="broadcastMessage" name="message" class="form-control" rows="5" placeholder="Введите текст. Можно использовать базовую Markdown-разметку ( *жирный*, _курсив_ )"></textarea>
                <small class="form-text text-muted">Сообщение будет отправлено всем активным пользователям.</small>
            </div>
            <div class="mb-3">
                <label for="broadcastAudio" class="form-label">Прикрепить аудиофайл (mp3)</label>
                <input type="file" id="broadcastAudio" name="audio" accept="audio/mpeg" class="form-control" />
            </div>
            <button type="submit" class="btn btn-success" onclick="return confirm('Вы уверены, что хотите запустить рассылку?')">
                <i class="fas fa-paper-plane"></i> Отправить рассылку
            </button>
        </form>
    </div>
</div>

<% if (typeof success !== 'undefined' && typeof error !== 'undefined') { %>
<div class="mt-4 alert alert-info">
    <h4>Результат последней рассылки:</h4>
    <p>✅ Успешно отправлено: <strong><%= success %></strong></p>
    <p>❌ Ошибок (пользователи заблокировали бота): <strong><%= error %></strong></p>
</div>
<% } %>