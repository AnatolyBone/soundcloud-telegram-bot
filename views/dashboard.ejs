<!DOCTYPE html>
<html>
<head>
  <title>Admin Panel</title>
  <meta charset="UTF-8" />
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body>
  <h1>📊 Admin Dashboard</h1>

  <p>Total users: <%= stats.totalUsers %></p>
  <p>Total downloads: <%= stats.totalDownloads %></p>
  <p>Free: <%= stats.free %>, Plus: <%= stats.plus %>, Pro: <%= stats.pro %>, Unlim: <%= stats.unlimited %></p>

  <h2>📈 Регистрация пользователей (по дням)</h2>
  <canvas id="registrationsChart" width="600" height="250"></canvas>

  <h2>📥 Скачивания треков (по дням)</h2>
  <canvas id="downloadsChart" width="600" height="250"></canvas>

  <h3>👤 Все пользователи (<%= users.length %>)</h3>
  <table border="1" cellpadding="5">
    <thead>
      <tr>
        <th>ID</th>
        <th>Username</th>
        <th>Имя</th>
        <th>Скачано</th>
        <th>Лимит</th>
        <th>Дата регистрации</th>
        <th>Последняя активность</th>
        <th>Сменить тариф</th>
      </tr>
    </thead>
    <tbody>
      <% users.slice().reverse().forEach(user => { %>
        <tr>
          <td><%= user.id %></td>
          <td>@<%= user.username %></td>
          <td><%= user.first_name %></td>
          <td><%= user.downloads_today %></td>
          <td><%= user.premium_limit %></td>
          <td><%= user.created_at ? new Date(user.created_at).toLocaleDateString() : '—' %></td>
          <td><%= user.last_active ? new Date(user.last_active).toLocaleDateString() : '—' %></td>
          <td>
            <form method="POST" action="/set-tariff">
              <input type="hidden" name="userId" value="<%= user.id %>">
              <select name="limit">
                <option value="10" <%= user.premium_limit === 10 ? 'selected' : '' %>>🆓 Free</option>
                <option value="50" <%= user.premium_limit === 50 ? 'selected' : '' %>>🎯 Plus</option>
                <option value="100" <%= user.premium_limit === 100 ? 'selected' : '' %>>💪 Pro</option>
                <option value="1000" <%= user.premium_limit >= 1000 ? 'selected' : '' %>>💎 Unlim</option>
              </select>
              <button type="submit">Сохранить</button>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>

  <h3>📢 Массовая рассылка</h3>
  <form method="POST" action="/broadcast">
    <textarea name="message" rows="4" cols="60" placeholder="Введите текст сообщения" required></textarea><br>
    <button type="submit">Отправить всем</button>
  </form>

  <h3>📝 Последние отзывы:</h3>
  <ul>
    <% if (reviews && reviews.length > 0) { %>
      <% reviews.forEach(r => { %>
        <li><strong><%= new Date(r.time).toLocaleString() %></strong>: <%= r.text %></li>
      <% }) %>
    <% } else { %>
      <li>Отзывы отсутствуют</li>
    <% } %>
  </ul>

  <script>
    const labels = <%- JSON.stringify(Object.keys(stats.registrationsByDate)) %>;

    const registrationsData = {
      labels,
      datasets: [{
        label: 'Регистрации',
        data: <%- JSON.stringify(Object.values(stats.registrationsByDate)) %>,
        borderColor: 'blue',
        backgroundColor: 'lightblue',
        tension: 0.3
      }]
    };

    const downloadsData = {
      labels,
      datasets: [{
        label: 'Скачивания',
        data: <%- JSON.stringify(Object.values(stats.downloadsByDate)) %>,
        borderColor: 'green',
        backgroundColor: 'lightgreen',
        tension: 0.3
      }]
    };

    new Chart(document.getElementById('registrationsChart'), {
      type: 'line',
      data: registrationsData,
      options: { responsive: true }
    });

    new Chart(document.getElementById('downloadsChart'), {
      type: 'line',
      data: downloadsData,
      options: { responsive: true }
    });
  </script>
</body>
</html>