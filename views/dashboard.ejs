<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8"/>
  <title>Admin Dashboard</title>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>

  <!-- Bootstrap 5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <!-- AdminLTE 4 -->
  <link href="https://cdn.jsdelivr.net/npm/admin-lte@4.0.2/dist/css/adminlte.min.css" rel="stylesheet"/>
  <!-- FontAwesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet"/>
  <!-- DataTables -->
  <link href="https://cdn.datatables.net/1.13.6/css/dataTables.bootstrap5.min.css" rel="stylesheet"/>
  <!-- Chart.js -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <style>
    .content-wrapper { min-height: 90vh; }
  </style>
</head>
<body class="hold-transition sidebar-mini">
<div class="wrapper">

  <!-- Top Navbar -->
  <nav class="main-header navbar navbar-expand-lg navbar-light border-bottom">
    <div class="container-fluid">
      <button class="btn btn-light" data-widget="pushmenu"><i class="fas fa-bars"></i></button>
      <a href="/dashboard" class="navbar-brand ms-3">Главная</a>
      <div class="ms-auto">
        <a href="/logout" class="btn btn-outline-secondary"><i class="fas fa-sign-out-alt"></i> Выход</a>
      </div>
    </div>
  </nav>

  <!-- Sidebar -->
  <aside class="main-sidebar sidebar-dark-primary elevation-4">
    <a href="/dashboard" class="brand-link">
      <i class="fas fa-music ms-2"></i>
      <span class="brand-text fw-light ms-2">BAZA Admin</span>
    </a>
    <div class="sidebar">
      <nav class="mt-2">
        <ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview">
          <li class="nav-item">
            <a href="/dashboard" class="nav-link <%= page === 'dashboard' ? 'active' : '' %>">
              <i class="nav-icon fas fa-tachometer-alt"></i>
              <p>Панель управления</p>
            </a>
          </li>
          <li class="nav-item">
            <a href="/users" class="nav-link <%= page === 'users' ? 'active' : '' %>">
              <i class="nav-icon fas fa-users"></i>
              <p>Пользователи</p>
            </a>
          </li>
          <li class="nav-item">
            <a href="/broadcast" class="nav-link <%= page === 'broadcast' ? 'active' : '' %>">
              <i class="nav-icon fas fa-bullhorn"></i>
              <p>Массовая рассылка</p>
            </a>
          </li>
          <li class="nav-item">
            <a href="/settings" class="nav-link <%= page === 'settings' ? 'active' : '' %>">
              <i class="nav-icon fas fa-cogs"></i>
              <p>Настройки</p>
            </a>
          </li>
        </ul>
      </nav>
    </div>
  </aside>

  <!-- Content wrapper -->
  <div class="content-wrapper">
    <!-- Header -->
    <header class="content-header bg-white border-bottom py-3">
      <div class="container-fluid d-flex justify-content-between align-items-center">
        <h1 class="m-0">📊 Админка</h1>
        <small>Пользователь: <%= user ? user.username || user.first_name : 'Гость' %></small>
      </div>
    </header>

    <!-- Main content -->
    <section class="content py-3">
      <div class="container-fluid">
        <!-- Tabs -->
        <ul class="nav nav-tabs mb-3" id="dashboardTabs" role="tablist">
          <li class="nav-item" role="presentation">
            <button class="nav-link active" id="stats-tab" data-bs-toggle="tab" data-bs-target="#stats" type="button">Статистика</button>
          </li>
          <li class="nav-item" role="presentation">
            <button class="nav-link" id="users-tab" data-bs-toggle="tab" data-bs-target="#usersTab" type="button">Пользователи</button>
          </li>
          <li class="nav-item" role="presentation">
            <button class="nav-link" id="broadcast-tab" data-bs-toggle="tab" data-bs-target="#broadcastTab" type="button">Рассылка</button>
          </li>
        </ul>

        <div class="tab-content" id="dashboardTabsContent">
          <!-- Статистика -->
          <div class="tab-pane fade show active" id="stats">
            <form class="d-flex mb-3" method="GET" action="/dashboard">
              <div class="form-check form-switch me-3">
                <input class="form-check-input" type="checkbox" id="showInactive" name="showInactive" value="true" <%= showInactive ? 'checked' : '' %>>
                <label class="form-check-label" for="showInactive">Показать неактивных</label>
              </div>
              <button class="btn btn-primary btn-sm me-2">Обновить</button>
              <a href="/export" class="btn btn-secondary btn-sm">Экспорт в CSV</a>
            </form>

            <div class="row mb-4">
              <div class="col-md-4">
                <div class="card bg-info text-white">
                  <div class="card-body d-flex align-items-center">
                    <i class="fas fa-users fa-2x me-3"></i>
                    <div>
                      <div>Всего пользователей</div>
                      <h3><%= stats.totalUsers %></h3>
                    </div>
                  </div>
                </div>
              </div>
              <div class="col-md-4">
                <div class="card bg-success text-white">
                  <div class="card-body d-flex align-items-center">
                    <i class="fas fa-download fa-2x me-3"></i>
                    <div>
                      <div>Всего загрузок</div>
                      <h3><%= stats.totalDownloads %></h3>
                    </div>
                  </div>
                </div>
              </div>
              <div class="col-md-4">
                <div class="card bg-warning text-white">
                  <div class="card-body">
                    <div>Тарифы (Free/Plus/Pro/Unlim)</div>
                    <h5>Free: <%= stats.free %>, Plus: <%= stats.plus %>, Pro: <%= stats.pro %>, Unlim: <%= stats.unlimited %></h5>
                  </div>
                </div>
              </div>
            </div>

            <!-- Графики -->
            <div class="card mb-4">
              <div class="card-header">Регистрации · Загрузки · Активные</div>
              <div class="card-body"><canvas id="combinedChart" height="200"></canvas></div>
            </div>

            <div class="row mb-4">
              <div class="col-md-6">
                <div class="card mb-3">
                  <div class="card-header">Активность по часам</div>
                  <div class="card-body"><canvas id="hourActivityChart" height="200"></canvas></div>
                </div>
              </div>
              <div class="col-md-6">
                <div class="card mb-3">
                  <div class="card-header">Активность по дням недели</div>
                  <div class="card-body"><canvas id="weekdayActivityChart" height="200"></canvas></div>
                </div>
              </div>
            </div>

            <div class="card mb-4">
              <div class="card-header">Тепловая карта активности</div>
              <div class="card-body"><canvas id="heatmapChart" height="300"></canvas></div>
              <details class="mt-2">
                <summary>Показать данные (JSON)</summary>
                <pre><%= JSON.stringify(activityByDayHour || {}, null, 2) %></pre>
              </details>
            </div>

            <div class="card border-danger mb-4">
              <div class="card-header bg-danger text-white">Тарифы, истекающие в ближайшие 3 дня</div>
              <div class="card-body">
                <% if (!expiringSoon.length) { %>
                  <p>Нет пользователей</p>
                <% } else { %>
                  <ul class="list-group">
                    <% expiringSoon.forEach(u => { %>
                      <li class="list-group-item d-flex justify-content-between align-items-center">
                        <span><%= u.first_name %> (<%= u.username ? '@'+u.username : '—' %>)</span>
                        <span>До <%= new Date(u.premium_until).toLocaleDateString() %></span>
                      </li>
                    <% }) %>
                  </ul>
                  <nav class="mt-3">
                    <ul class="pagination justify-content-center">
                      <% if (expiringOffset>0) { %>
                        <li class="page-item"><button class="page-link" onclick="changePage(-1)">← Назад</button></li>
                      <% } %>
                      <% if (expiringOffset+expiringLimit<expiringCount) { %>
                        <li class="page-item"><button class="page-link" onclick="changePage(1)">Вперёд →</button></li>
                      <% } %>
                    </ul>
                    <p class="text-center text-muted">Показано <%= expiringOffset+1 %>–<%= Math.min(expiringOffset+expiringLimit, expiringCount) %> из <%= expiringCount %></p>
                  </nav>
                <% } %>
              </div>
            </div>

            <div class="card mb-4">
              <div class="card-header">Источники трафика</div>
              <div class="card-body p-0">
                <table class="table table-striped table-hover mb-0">
                  <thead>
                    <tr><th>Источник</th><th>Кол-во</th></tr>
                  </thead>
                  <tbody>
                    <% referralStats.forEach(s => { %>
                      <tr><td><%= s.source||'—' %></td><td><%= s.count %></td></tr>
                    <% }) %>
                  </tbody>
                </table>
              </div>
            </div>
          </div>

          <!-- Пользователи -->
          <div class="tab-pane fade" id="usersTab">
            <div class="table-responsive">
              <table id="usersTable" class="table table-bordered table-hover">
                <thead>
                  <tr><th>ID</th><th>Username</th><th>Имя</th><th>Скачано</th><th>Лимит</th><th>Регистрация</th><th>Активность</th><th>Статус</th><th>Источник</th><th>Тариф</th></tr>
                </thead>
                <tbody>
                  <% users.forEach(u => { %>
                    <tr class="<%= !u.active?'table-secondary':'' %>">
                      <td><%= u.id %></td>
                      <td><%= u.username?'@'+u.username:'' %></td>
                      <td>
                        <%= u.first_name %>
                        <% if (new Date(u.created_at) > Date.now()-86400000) { %>
                          <span class="badge bg-info">🆕</span>
                        <% } %>
                      </td>
                      <td><%= u.total_downloads||0 %></td>
                      <td><%= u.premium_limit %></td>
                      <td><%= u.created_at?new Date(u.created_at).toLocaleDateString():'-' %></td>
                      <td><%= u.last_active?new Date(u.last_active).toLocaleDateString():'-' %></td>
                      <td>
                        <% if (!u.last_active) { %>🔴
                        <% } else if (new Date(u.last_active)>Date.now()-86400000) { %>🟢
                        <% } else { %>🟡
                        <% } %>
                      </td>
                      <td><%= u.referral_source||'—' %></td>
                      <td>
                        <form method="POST" action="/set-tariff" class="d-flex align-items-center">
                          <input type="hidden" name="userId" value="<%= u.id %>">
                          <select name="limit" class="form-select form-select-sm me-2">
                            <option value="10" <%= u.premium_limit===10?'selected':'' %>>Free</option>
                            <option value="50" <%= u.premium_limit===50?'selected':'' %>>Plus</option>
                            <option value="100" <%= u.premium_limit===100?'selected':'' %>>Pro</option>
                            <option value="1000" <%= u.premium_limit>=1000?'selected':'' %>>Unlimited</option>
                          </select>
                          <button class="btn btn-primary btn-sm">Сохранить</button>
                        </form>
                      </td>
                    </tr>
                  <% }) %>
                </tbody>
              </table>
            </div>
          </div>

          <!-- Рассылка -->
          <div class="tab-pane fade" id="broadcastTab">
            <form action="/broadcast" method="POST" enctype="multipart/form-data">
              <div class="mb-3">
                <label for="broadcastMessage" class="form-label">Сообщение</label>
                <textarea id="broadcastMessage" name="message" class="form-control" rows="4" placeholder="Введите текст"></textarea>
              </div>
              <div class="mb-3">
                <label for="broadcastAudio" class="form-label">Аудиофайл (mp3)</label>
                <input id="broadcastAudio" type="file" name="audio" accept="audio/mp3" class="form-control"/>
              </div>
              <button class="btn btn-success">Отправить</button>
            </form>
          </div>
        </div>
      </div>
    </section>
  </div>

  <!-- Footer -->
  <footer class="main-footer text-center small py-2 border-top">
    <strong>© 2025 BAZA</strong> — AdminLTE 4 / Bootstrap 5
  </footer>
</div>

<!-- Scripts -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/admin-lte@4.0.2/dist/js/adminlte.min.js"></script>
<script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
<script src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js"></script>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    $('#usersTable').DataTable({ responsive: true, order: [[5, 'desc']], language: {
      search: "Поиск:", lengthMenu: "Показать _MENU_", info: "Показано _START_–_END_ из _TOTAL_", paginate: { first: "Первая", last: "Последняя", next: "→", previous: "←" },
      zeroRecords: "Нет данных"
    }});

    // Графики остаются без изменений
    // ...
    window.changePage = delta => {
      const params = new URLSearchParams(location.search);
      const limit = params.get('expiringLimit') || '<%= expiringLimit %>';
      const offset = parseInt(params.get('expiringOffset') || '0', 10) + delta * parseInt(limit, 10);
      params.set('expiringOffset', offset < 0 ? 0 : offset);
      location.search = params.toString();
    };
  });
</script>

</body>
</html>